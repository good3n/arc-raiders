
[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Route frontend-friendly /api/* to our Netlify function (serverless CORS proxy)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/proxy/:splat"
  status = 200
  force = true
